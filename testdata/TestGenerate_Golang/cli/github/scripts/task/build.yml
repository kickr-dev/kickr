# Code generated by kickr; DO NOT EDIT.

version: "3"

tasks:
  test:
    desc: run unit tests.
    cmds:
      - go test ./... -count 1 -timeout=30s
  test-race:
    desc: run unit tests with the race detector.
    cmds:
      - CGO_ENABLED="1" go test ./... -race -timeout=30s
  test-cover:
    desc: run unit tests with coverage.
    cmds:
      - go test ./... -v -coverpkg="./..." -covermode="count" -timeout=30s
  build:
    desc: build the name binary.
    vars:
      APP_VERSION: '{{ env "APP_VERSION" | default "v0.0.0" }}'
      GIT_BRANCH:
        sh: git rev-parse --abbrev-ref HEAD
      GIT_COMMIT:
        sh: git rev-parse HEAD
      BUILD_DATE:
        sh: TZ=UTC date '+%Y-%m-%dT%TZ'
    cmds:
      - |-
        CGO_ENABLED="0" go build \
          -ldflags "\
            -X 'github.com/kickr-dev/kickr/internal/build.branch={{ .GIT_BRANCH }}' \
            -X 'github.com/kickr-dev/kickr/internal/build.commit={{ .GIT_COMMIT }}' \
            -X 'github.com/kickr-dev/kickr/internal/build.date={{ .BUILD_DATE }}' \
            -X 'github.com/kickr-dev/kickr/internal/build.version={{ .APP_VERSION }}' \
          " \
          -o name ./cmd/name
