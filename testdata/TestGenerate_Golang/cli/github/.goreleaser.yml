# Code generated by kickr; DO NOT EDIT.

version: 2

checksum:
  name_template: checksums.txt
snapshot:
  version_template: "{{ .Env.VERSION }}"

builds:
  - main: ./cmd/name
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X github.com/kickr-dev/kickr/internal/build.branch={{ .Branch }}
      - -X github.com/kickr-dev/kickr/internal/build.commit={{ .Commit }}
      - -X github.com/kickr-dev/kickr/internal/build.date={{ .Date }}
      - -X github.com/kickr-dev/kickr/internal/build.version={{ .Env.VERSION }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip

nfpms:
  - maintainer: kilianpaquier
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - deb
      - rpm
