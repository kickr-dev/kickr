# Code generated by kickr; DO NOT EDIT.

default_install_hook_types:
  - commit-msg
  - pre-commit

default_stages:
  - pre-commit

repos:
  - repo: https://github.com/compilerla/conventional-pre-commit
    # renovate: datasource=github-tags packageName=compilerla/conventional-pre-commit depName=conventional-pre-commit
    rev: v4.2.0
    hooks:
      - id: conventional-pre-commit
        stages: [commit-msg]

  - repo: https://github.com/gitleaks/gitleaks
    # renovate: datasource=github-tags packageName=gitleaks/gitleaks depName=gitleaks
    rev: v8.28.0
    hooks:
      - id: gitleaks

  - repo: https://github.com/pre-commit/pre-commit-hooks
    # renovate: datasource=github-tags packageName=pre-commit/pre-commit-hooks depName=pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: no-commit-to-branch
        args: ['--pattern', '^(?!((dev|docs|fix)(\/[\w-]+){1,2})$|master|main|v[0-9]+\.x|v[0-9]+\.[0-9]+\.x|next|alpha|beta|staging|dev|develop|development).*']
      - id: trailing-whitespace

  - repo: local
    hooks:
      - id: tidy
        name: go mod tidy
        entry: bash -c 'go mod tidy'
        language: system

  - repo: https://github.com/golangci/golangci-lint
    # renovate: datasource=github-tags packageName=golangci/golangci-lint depName=golangci-lint
    rev: v2.5.0
    hooks:
      - id: golangci-lint
