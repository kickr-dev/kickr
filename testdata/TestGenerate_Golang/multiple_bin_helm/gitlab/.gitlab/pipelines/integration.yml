# Code generated by kickr; DO NOT EDIT.

---
include:

  # pre-commit template
  # https://gitlab.com/to-be-continuous/pre-commit
  - component: $CI_SERVER_FQDN/to-be-continuous/pre-commit/gitlab-ci-pre-commit@805b30e6daceca57eae1296f7864bd65577c0b40 # 1.5.0
    inputs:
      pre-commit-file: .pre-commit-config.yaml

  # Go template
  # https://gitlab.com/to-be-continuous/golang
  - component: $CI_SERVER_FQDN/to-be-continuous/golang/gitlab-ci-golang@8a3028125ed0e16d9ad1be4fee6c2905e64915b0 # 4.16.0
    inputs:
      build-mode: modules
      ci-lint-args: --config=.golangci.yml --timeout=300s --allow-parallel-runners --output.checkstyle.path="reports/go-ci-lint.checkstyle.xml" --output.text.path=stdout
      sbom-opts: -licenses -assert-licenses
      test-flags: -v -coverpkg=./... -covermode=count -timeout=30s
      test-image: $GO_IMAGE

  # SonarQube template
  # https://gitlab.com/to-be-continuous/sonar
  - component: $CI_SERVER_FQDN/to-be-continuous/sonar/gitlab-ci-sonar@6f2eb1a3265e186575b49578b97832e0ca1c2ef6 # 5.2.2
    inputs:
      base-args: |
        -Dsonar.properties=sonar.properties
        -Dsonar.links.homepage=$CI_PROJECT_URL
        -Dsonar.links.ci=$CI_PROJECT_URL/-/pipelines
        -Dsonar.links.issue=$CI_PROJECT_URL/-/issues
      quality-gate-enabled: "true"

go-govulncheck:
  allow_failure: true

go-build:
  rules: [when: never]
