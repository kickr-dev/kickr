# Code generated by kickr; DO NOT EDIT.

---
include:

  # pre-commit template
  # https://gitlab.com/to-be-continuous/pre-commit
  - component: gitlab.com/to-be-continuous/pre-commit/gitlab-ci-pre-commit@781cb6e8a6a4009a4ca29357fd6aa1a81f9867cd # 1.4.2
    inputs:
      pre-commit-file: .pre-commit-config.yaml

  # Go template
  # https://gitlab.com/to-be-continuous/golang
  - component: gitlab.com/to-be-continuous/golang/gitlab-ci-golang@473da325602b2a247fc6d880bc243cf6a42dbe0f # 4.15.3
    inputs:
      build-mode: modules
      ci-lint-args: --config=.golangci.yml --timeout=240s --allow-parallel-runners --output.checkstyle.path="reports/go-ci-lint.checkstyle.xml" --output.text.path=stdout
      sbom-opts: -licenses -assert-licenses
      test-flags: -v -coverpkg=./... -covermode=count -timeout=30s
      test-image: $GO_IMAGE

  # SonarQube template
  # https://gitlab.com/to-be-continuous/sonar
  - component: gitlab.com/to-be-continuous/sonar/gitlab-ci-sonar@d634d32af33537fd47106ada357051f1abb136ca # 5.0.2
    inputs:
      base-args: |
        -Dsonar.properties=sonar.properties
        -Dsonar.links.homepage=$CI_PROJECT_URL
        -Dsonar.links.ci=$CI_PROJECT_URL/-/pipelines
        -Dsonar.links.issue=$CI_PROJECT_URL/-/issues
      quality-gate-enabled: "true"

go-govulncheck:
  allow_failure: true

go-build:
  rules: [when: never]
