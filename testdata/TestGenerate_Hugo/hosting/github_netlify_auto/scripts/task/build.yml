# Code generated by kickr; DO NOT EDIT.

version: "3"

tasks:
  build:
    desc: build the hugo site.
    cmds:
      - hugo --gc --minify --destination dist --logLevel debug
  serve:
    desc: serve the hugo site locally.
    cmds:
      - hugo server --disableFastRender --destination dist --logLevel debug
  production:
    desc: serve the hugo site through netlify dev.
    cmds:
      - netlify dev -c "task serve" --target-port 1313 --port 8888 --dir dist --framework hugo --context production
