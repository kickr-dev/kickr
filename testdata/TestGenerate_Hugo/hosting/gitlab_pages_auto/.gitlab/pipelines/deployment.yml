# Code generated by kickr; DO NOT EDIT.

---
include: []

.pages:
  image: $PAGES_IMAGE
  stage: deploy
  environment:
    name: pages${PAGES_ENVIRONMENT_NAMESPACE}
    action: start
    url: $CI_PAGES_URL
  variables:
    GIT_STRATEGY: none
    PAGES_PROJECT_DIR: .
  rules:
    - if: $CI_COMMIT_REF_NAME != $CI_DEFAULT_BRANCH
      when: never
  pages:
    publish: $PAGES_PROJECT_DIR/$BUILD_DIR
  artifacts:
    name: "$CI_JOB_NAME artifacts from $CI_PROJECT_NAME on $CI_COMMIT_REF_SLUG"
    paths:
      - $PAGES_PROJECT_DIR/$BUILD_DIR
    expire_in: 1 day

pages:
  extends: .pages
  dependencies:
    - hugo-build
