# Code generated by kickr; DO NOT EDIT.

---
include: []

hugo-build:
  image:
    name: $HUGO_IMAGE
    entrypoint: [""]
  stage: build
  needs:
    - semantic-release-info
  parallel:
    matrix:
      - HUGO_PROJECT_DIR: docs
      - HUGO_PROJECT_DIR: website
  rules:
    - !reference [.test-policy, rules]
  before_script:
    - git config --global --add safe.directory "$CI_PROJECT_DIR"
  script:
    - cd "$HUGO_PROJECT_DIR"
    - hugo --gc --minify --destination "$BUILD_DIR" --logLevel "${LOG_LEVEL:-info}"
  artifacts:
    name: "$CI_JOB_NAME artifacts from $CI_PROJECT_NAME on $CI_COMMIT_REF_SLUG"
    paths:
      - $HUGO_PROJECT_DIR/$BUILD_DIR
    expire_in: 1 day
