# Code generated by kickr; DO NOT EDIT.

---
.netlify-deploy:
  image: $NETLIFY_IMAGE
  stage: deploy
  variables:
    GIT_STRATEGY: none
  before_script:
    - npm install -g netlify-cli
    - apk add --no-cache jq
  script:
    - netlify deploy --site "$NETLIFY_SITE_ID" --auth "$NETLIFY_AUTH_TOKEN" --dir "$BUILD_DIR" $ARGS --json > netlify-deploy.json
    - echo "ENVIRONMENT_URL=$(jq -r '.url // .deploy_url' netlify-deploy.json)" >> netlify.out.env
  artifacts:
    reports:
      dotenv: netlify.out.env

.netlify-cleanup:
  image: $NETLIFY_IMAGE
  stage: deploy
  dependencies: []
  variables:
    GIT_STRATEGY: none
  before_script:
    - npm install -g netlify-cli
    - apk add --no-cache jq
  script:
    - |
      netlify api listSiteDeploys \
        --auth "$NETLIFY_AUTH_TOKEN" \
        --data "{\"site_id\": \"$NETLIFY_SITE_ID\", \"branch\": \"$BRANCH_SHA\"}" > netlify-deploys.json
    - |
      for id in $(jq -r '.[].id' netlify-deploys.json); do
        netlify api deleteSiteDeploy --auth "$NETLIFY_AUTH_TOKEN" --data "{\"site_id\": \"$NETLIFY_SITE_ID\", \"deploy_id\": \"$id\"}"
      done

netlify-deploy-review-docs:
  extends: .netlify-deploy
  environment:
    name: review${NETLIFY_ENVIRONMENT_NAMESPACE}/${CI_COMMIT_REF_NAME}
    action: start
    on_stop: netlify-cleanup-review-docs
    url: $ENVIRONMENT_URL
  dependencies:
    - "hugo-build: [docs]"
    - semantic-release-info
  rules:
    - if: '$CI_COMMIT_REF_NAME =~ $PROD_REF'
      when: never
    - when: manual
      allow_failure: true
  variables:
    ARGS: --alias=$BRANCH_SHA --context branch:$BRANCH_SHA --message=$CI_COMMIT_REF_NAME
    NETLIFY_ENVIRONMENT_NAMESPACE: /docs

netlify-cleanup-review-docs:
  extends: .netlify-cleanup
  environment:
    name: review${NETLIFY_ENVIRONMENT_NAMESPACE}/${CI_COMMIT_REF_NAME}
    action: stop
  rules:
    - if: '$CI_COMMIT_REF_NAME =~ $PROD_REF'
      when: never
    - when: manual
      allow_failure: true

netlify-deploy-production-docs:
  extends: .netlify-deploy
  environment:
    name: production${NETLIFY_ENVIRONMENT_NAMESPACE}
    action: start
    url: $ENVIRONMENT_URL
  dependencies:
    - "hugo-build: [docs]"
    - semantic-release-info
  rules:
    - if: '$CI_COMMIT_REF_NAME !~ $PROD_REF'
      when: never
    - when: manual
  variables:
    ARGS: --prod --message=$CI_COMMIT_REF_NAME
    NETLIFY_ENVIRONMENT_NAMESPACE: /docs

netlify-deploy-review-website:
  extends: .netlify-deploy
  environment:
    name: review${NETLIFY_ENVIRONMENT_NAMESPACE}/${CI_COMMIT_REF_NAME}
    action: start
    on_stop: netlify-cleanup-review-website
    url: $ENVIRONMENT_URL
  dependencies:
    - "hugo-build: [website]"
    - semantic-release-info
  rules:
    - if: '$CI_COMMIT_REF_NAME =~ $PROD_REF'
      when: never
    - when: manual
      allow_failure: true
  variables:
    ARGS: --alias=$BRANCH_SHA --context branch:$BRANCH_SHA --message=$CI_COMMIT_REF_NAME
    NETLIFY_ENVIRONMENT_NAMESPACE: /website

netlify-cleanup-review-website:
  extends: .netlify-cleanup
  environment:
    name: review${NETLIFY_ENVIRONMENT_NAMESPACE}/${CI_COMMIT_REF_NAME}
    action: stop
  rules:
    - if: '$CI_COMMIT_REF_NAME =~ $PROD_REF'
      when: never
    - when: manual
      allow_failure: true

netlify-deploy-production-website:
  extends: .netlify-deploy
  environment:
    name: production${NETLIFY_ENVIRONMENT_NAMESPACE}
    action: start
    url: $ENVIRONMENT_URL
  dependencies:
    - "hugo-build: [website]"
    - semantic-release-info
  rules:
    - if: '$CI_COMMIT_REF_NAME !~ $PROD_REF'
      when: never
    - when: manual
  variables:
    ARGS: --prod --message=$CI_COMMIT_REF_NAME
    NETLIFY_ENVIRONMENT_NAMESPACE: /website
