# Code generated by kickr; DO NOT EDIT.

---
include:

  # pre-commit template
  # https://gitlab.com/to-be-continuous/pre-commit
  - component: gitlab.com/to-be-continuous/pre-commit/gitlab-ci-pre-commit@805b30e6daceca57eae1296f7864bd65577c0b40 # 1.5.0
    inputs:
      pre-commit-file: .pre-commit-config.yaml

  # Bash template
  # https://gitlab.com/to-be-continuous/bash
  - component: gitlab.com/to-be-continuous/bash/gitlab-ci-bash@25e0ab61ffadef08b7cda4032afbaa859aebc280 # 3.10.0
    inputs:
      bats-enabled: "false"
      shellcheck-files: "**/*.*sh"

pre-commit:
  variables:
    GIT_DEPTH: "0"
    GIT_STRATEGY: clone
  before_script:
    # see https://gitlab.com/to-be-continuous/pre-commit/-/blob/main/templates/gitlab-ci-pre-commit.yml?ref_type=heads#L430
    - !reference [.pre-commit-scripts]
    - install_ca_certs "${CUSTOM_CA_CERTS:-$DEFAULT_CA_CERTS}"
    - apk add git
    - git checkout -b "$CI_COMMIT_REF_NAME"
