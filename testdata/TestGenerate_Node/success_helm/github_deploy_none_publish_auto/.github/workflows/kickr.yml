# Code generated by kickr; DO NOT EDIT.

name: Kickr
run-name: Kickr

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * *"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  kickr:
    name: Kickr
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00 # v6.0.0
        with:
          check-latest: true
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: kickr-dev/action-setup@1abe1e8ec8b6109427dad81001b0018cb454529c # v0.0.0
        with:
          install-mode: goinstall
          version: beta
      - run: kickr generate
      - uses: peter-evans/create-pull-request@271a8d0340265f705b14b6d32b9829c1cb33d45e # v7.0.8
        with:
          assignees: |
            kilianpaquier
          body: "Automated kickr layout generation"
          branch: kickr/layout
          commit-message: "ci(layout): regenerate kickr layout"
          reviewers: |
            kilianpaquier
          sign-commits: true
          signoff: true
          title: "ci(layout): regenerate kickr layout"
