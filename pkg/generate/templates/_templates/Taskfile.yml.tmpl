# Code generated by kickr; DO NOT EDIT.

version: "3"

silent: true

tasks:
  default:
    desc: list available tasks.
    cmds:
      - task --list

includes:
<<- if or (hasKey .Languages "hugo") (hasKey .Languages "go") (hasKey .Languages "terraform") .HasDocker >>
  build:
    taskfile: ./scripts/task/build.yml
    flatten: true
<<- end >>
  custom:
    taskfile: ./scripts/task/custom.yml
    optional: true
    flatten: true

<<- if eq .Dir "." >>
<<- $go_modules := .Parent.ModulesWith "go" >>
<<- $terraform_modules := .Parent.ModulesWith "terraform" >>
  clean:
    taskfile: ./scripts/task/clean.yml
    flatten: true

<<- if or (gt (len $go_modules) 0) (gt (len $terraform_modules) 0) >>
  lint:
    taskfile: ./scripts/task/lint.yml
    flatten: true
<<- end >>

<<- range .Parent.Modules >>
<<- if and (ne .Dir ".") (.HasBuildTool "task") >>
  << .Slug >>:
    dir: ./<< .Dir >>
    taskfile: ./<< .Dir >>/Taskfile.yml
<<- end >>
<<- end >>
<<- end >>
