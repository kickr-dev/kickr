# Code generated by kickr; DO NOT EDIT.

<<- $imports := list >>
<<- $modules := list >>

<<- $go_modules := .Parent.ModulesWith "go" >>
<<- $terraform_modules := .Parent.ModulesWith "terraform" >>

<<- if or (hasKey .Languages "hugo") (hasKey .Languages "go") (hasKey .Languages "terraform") .HasDocker >>
<<- $imports = append $imports (printf "import %q" "scripts/just/build.just") >>
<<- end >>

<<- if eq .Dir "." >>
<<- $imports = append $imports (printf "import %q" "scripts/just/clean.just") >>
<<- end >>
<<- $imports = append $imports (printf "import? %q" "scripts/just/custom.just") >>

<<- if eq .Dir "." >>
<<- if or (gt (len $go_modules) 0) (gt (len $terraform_modules) 0) >><<- $imports = append $imports (printf "import %q" "scripts/just/lint.just") >><<- end >>
<<- range .Parent.Modules >><<- if and (ne .Dir ".") (.HasBuildTool "just") >><<- $modules = append $modules . >><<- end >><<- end >>
<<- end >>

<<- if gt (len $imports) 0 >><< print "\n" >><<- end >><</* simple ugly pretty print */>>
<<- range $imports >>
<< . >>
<<- end >>

<<- if gt (len $modules) 0 >><< print "\n" >><<- end >><</* simple ugly pretty print */>>
<<- range $modules >>
# << .Dir >> module commands.
mod << .Slug >> "<< .Dir >>/Justfile"
<<- end >>

# list available recipes.
default:
    @just --list
