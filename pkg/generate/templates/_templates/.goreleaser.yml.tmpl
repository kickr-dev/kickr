# Code generated by kickr; DO NOT EDIT.

<<- $specifics := get .Languages "go" >>

version: 2

checksum:
  name_template: checksums.txt
snapshot:
  version_template: "{{ .Env.APP_VERSION }}"

builds:
<<- range $name, $config := .Clis >>
  - main: ./cmd/<< $name >>
    env:
      - CGO_ENABLED=0
    ldflags:
      - -X << $specifics.Module >>/internal/build.branch={{ .Branch }}
      - -X << $specifics.Module >>/internal/build.commit={{ .Commit }}
      - -X << $specifics.Module >>/internal/build.date={{ .Date }}
      - -X << $specifics.Module >>/internal/build.version={{ .Env.APP_VERSION }}
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
<<- end >>

archives:
  - formats: tar.gz
    wrap_in_directory: false
    name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip

nfpms:
<<- $maintainer := index .Maintainers 0 >>
  - maintainer: << $maintainer.Name >>
<<- if .License >>
    license: << .License | upper >>
<<- end >>
    file_name_template: >-
      {{- .ProjectName }}_
      {{- .Os }}_
      {{- .Arch }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    formats:
      - deb
      - rpm
