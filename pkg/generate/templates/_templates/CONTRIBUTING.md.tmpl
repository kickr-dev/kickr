<!-- Code generated by kickr; DO NOT EDIT. -->

# Contributing

Thanks for taking the time to contribute!

{{- if not (has "code-of-conduct" .Config.Exclude) }}

By participating in this project you agree to abide by the [Code of Conduct](CODE_OF_CONDUCT.md).
{{- end }}

## How to contribute

{{- if eq .Config.Platform "gitlab" }}

1. Search [existing work items](https://{{ .VCS.ProjectHost }}/{{ .VCS.ProjectPath }}/-/work_items) before opening a new one.
2. Fork the repository and create a branch off the default branch.
{{- if has "gitflow-branches" .Config.PreCommit }}
  Name it `<type>/<slug>` with `<type>` one of chore/docs/feat/fix/release (e.g. `feat/my-feature`), pre-commit rejects other names.
{{- end }}
3. Make your change, add or update tests (extend existing tests whenever possible), update docs.
4. Open a [merge request](https://{{ .VCS.ProjectHost }}/{{ .VCS.ProjectPath }}/-/merge_requests).
{{- end }}

{{- if eq .Config.Platform "github" }}

1. Search [existing issues](https://{{ .VCS.ProjectHost }}/{{ .VCS.ProjectPath }}/issues) before opening a new one.
2. Fork the repository and create a branch off the default branch.
{{- if has "gitflow-branches" .Config.PreCommit }}
  Name it `<type>/<slug>` with `<type>` one of chore/docs/feat/fix/release (e.g. `feat/my-feature`), pre-commit rejects other names.
{{- end }}
3. Make your change, add or update tests (extend existing tests whenever possible), update docs.
4. Open a [pull request](https://{{ .VCS.ProjectHost }}/{{ .VCS.ProjectPath }}/pulls).
{{- end }}

Files with `Code generated by kickr; DO NOT EDIT.` header are regenerated on each layout update, manual edits get overwritten.
Remove the header to opt a file out of regeneration, unless `kickr generate -f` is used.

## Development setup

{{- $engine := .Config.TerraformEngine }}

{{- if .HasMakefile }}

Global builds commands can be run with the following commands:
- `make lint`
- `make test`

{{- if gt (len .Modules) 1 }}

Specific modules builds commands can be run with the following commands:
- From repository's root directory:
  - `make <module-slug>-lint`
  - `make <module-slug>-test`
  - `make <module-slug>-build`
{{- if .HasDocker }}
  - `make <module-slug>-docker`
{{- end }}
- Within each module directory:
  - `make lint`
  - `make test`
  - `make build`
{{- if .HasDocker }}
  - `make docker`
{{- end }}
{{- end }}

{{- else }}

{{- $go_modules := .ModulesWith "go" }}
{{- if gt (len $go_modules) 0 }}
{{- $dirs := list }}
{{- range $go_modules }}{{ $dirs = append $dirs (ne .Dir "." | ternary .Dir "root") }}{{ end }}
{{- $plural := eq (len $dirs) 1 | ternary "directory" "directories" }}

For repository's {{ join ", " $dirs }} {{ $plural }}, lint and test commands can be run through:
- `golangci-lint run --config=.golangci.yml`
- `go test ./... -timeout=30s`
{{- if gt .Executables.Binaries 0 }}
- `go build -o <command> ./cmd/<command>`
{{- end }}
{{- end }}

{{- $hugo_modules := .ModulesWith "hugo" }}
{{- if gt (len $hugo_modules) 0 }}
{{- $dirs := list }}
{{- range $hugo_modules }}{{ $dirs = append $dirs (ne .Dir "." | ternary .Dir "root") }}{{ end }}
{{- $plural := eq (len $dirs) 1 | ternary "directory" "directories" }}

For repository's {{ join ", " $dirs }} {{ $plural }}, build commands can be run through:
- `hugo --gc --minify --destination dist`
- `hugo server --disableFastRender --destination dist`
{{- end }}

{{- $node_modules := .ModulesWith "node" }}
{{- if gt (len $node_modules) 0 }}
{{- $dirs := list }}
{{- range $node_modules }}{{ $dirs = append $dirs (ne .Dir "." | ternary .Dir "root") }}{{ end }}
{{- $firstnode := first $node_modules }}
{{- $manager := cutAfter (get $firstnode.Languages "node").PackageManager "@" }}
{{- $plural := eq (len $dirs) 1 | ternary "directory" "directories" }}

For repository's {{ join ", " $dirs }} {{ $plural }}, lint, test and build commands can be run through:
- `{{ $manager }} run lint`
- `{{ $manager }} run test`
- `{{ $manager }} run build`
{{- end }}

{{- $tf_modules := .ModulesWith "terraform" }}
{{- if gt (len $tf_modules) 0 }}
{{- $dirs := list }}
{{- range $tf_modules }}{{ $dirs = append $dirs (.Dir | default "root") }}{{ end }}
{{- $plural := eq (len $dirs) 1 | ternary "directory" "directories" }}

For repository's {{ join ", " $dirs }} {{ $plural }}, format and validation commands can be run through:
- `{{ $engine }} fmt -diff -recursive`
- `{{ $engine }} validate`
- `tflint --recursive`
{{- end }}

{{- end }}

## Commit messages

Commits must follow [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/): `type(scope): summary`.
A `!` after type/scope, or a `BREAKING CHANGE:` footer, forces a major release regardless of type.

| Type       | Purpose                                 | Release |
| ---------- | --------------------------------------- | ------- |
| `feat`     | New feature                             | minor   |
| `fix`      | Bug fix                                 | patch   |
| `perf`     | Performance improvement                 | patch   |
| `refactor` | Code change, no behavior change         | minor   |
| `revert`   | Reverts a previous commit               | patch   |
| `docs`     | Documentation only                      | patch   |
| `chore`    | Tooling, deps, config, preparatory work | patch   |
| `build`    | Build system or dependencies            | none    |
| `ci`       | CI configuration                        | none    |
| `style`    | Formatting, no code meaning change      | none    |
| `test`     | Adding or fixing tests                  | none    |

{{- if not (has "pre-commit" .Config.Exclude) }}

## Pre-commit hooks

This repo uses [pre-commit](https://pre-commit.com).
Install it and run `pre-commit install` once, hooks then run automatically on commit.
{{- end }}

## Code review

Every change goes through review before merge.
Keep commits separate and functional (one logical change per commit, no dumping-ground commits).
Force-pushing to update a review branch is fine.
