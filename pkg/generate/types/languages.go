package types //nolint:revive

// Languages names, as they're set by parsers in the modules languages,
// and as they're read by templates.
const (
	LanguageGo        = "go"
	LanguageHelm      = "helm"
	LanguageHugo      = "hugo"
	LanguageNode      = "node"
	LanguageTerraform = "terraform"
)
